# Server-only geheimen (DB-wachtwoord, ADMIN_WACHTWOORD, API-sleutel)
config.php

# Overig
.DS_Store
*.log
error_log

# Node-restant / lokale env
.env

# Privé brondocumenten (jaarrekeningen, bankafschriften, facturen) — NOOIT naar repo/server
assets/documenten/
facturen/
